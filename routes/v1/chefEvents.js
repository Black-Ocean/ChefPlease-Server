module.exports = function(app) {
  //get all chefs for a specific event
  app.get('/chefEvent/:id', function () {
    
  }); 

  //add supplied chef to event
  app.post('/chefEvent/:id', function () {

  });

  //delete all chefs for a specific event
  app.delete('/chefEvent/:id', function () {

  });
  //delete a specific chef for a specific event
  app.delete('/chefEvent/:id/:chefId', function () {

  });
}